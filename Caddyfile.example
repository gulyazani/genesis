# VPS'te Caddy. Hedef host: supershell.click
# BotFather Mini App URL ve webhook ile aynı tut.
# Webhook: https://supershell.click/api/telegram/webhook
# setWebhook'i yalnızca bu domain gerçekten bu VPS'e çözülünce çağır.

supershell.click {
  encode gzip
  reverse_proxy app:43127
}
